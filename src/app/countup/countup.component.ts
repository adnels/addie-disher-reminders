import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'app-countup',
  templateUrl: './countup.component.html',
  styleUrls: ['./countup.component.css']
})
export class CountupComponent implements OnInit {

  title = 'countup'
  count = 0

  onClick() {

    //const = numList();

    //numList.push(numList.length);

    //add number here -

    // console.log(numList);

    //need to add new line plus one while keeping last answer
    //create array, add a number to array on each c

  }

  constructor() { }

  ngOnInit(): void {
  }

}
